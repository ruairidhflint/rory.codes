<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
      html {
        width: 1200px;
        height: 630px;
        background-color: #fff;
        margin: 0;
        padding: 0;
      }
      body {
        width: 1200px; /* Standard OG image width */
        height: 630px; /* Standard OG image height */
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        font-family: 'Helvetica', sans-serif; /* Base font family */
        background-color: #fff; /* Ensure body itself has white background */
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        text-align: center;
      }
      .container {
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: flex-end;
        justify-content: flex-end;
        background-color: #fff; /* Explicit white background for container */
        position: relative; /* Existing: for absolute positioning of the symbol */
        padding: 50px; /* Added for spacing */
        box-sizing: border-box; /* Added for robust padding behavior */
      }
      .symbol {
        position: absolute; /* Added: to position in top-left */
        top: 50px;          /* Added: distance from top */
        left: 50px;         /* Added: distance from left */
        font-size: 200px;    /* Changed: from 48px to make it larger (asterisk char width ~30-35px) */
        color: black;       /* Existing */
        line-height: 1;     /* Added: to better control element height */
        /* margin-bottom: 20px; Removed: not needed for absolute positioning */
      }
      .title {
        font-family: 'Helvetica', Arial, sans-serif; /* Changed: for a slightly nicer look */
        font-weight: 500;   /* Changed: lighter font weight for a more refined feel */
        font-size: 50px;    /* Existing */
        color: black;       /* Existing */
        line-height: 1.3;   /* Existing */
        max-width: 90%;     /* Existing */
        text-align: right;
      }
      .site-name {
        display: none;      /* Existing */
      }
    </style>
</head>
<body>
  <div class="container">
    <div class="symbol">
      *
    </div>
    <div class="title">
      {{ title | safe }}
    </div>
  </div>
</body>
</html>
