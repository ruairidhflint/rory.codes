{% extends "base.njk" %}
{% block main %}
<a class="muted small" href="/">← rory flint</a>
<h1>{{ title }}</h1>
{{ content | safe }}

<br />
<ul>
  {% for item in portfolio %}
  <li>
    <a class="post-link" href="{{ item.url }}" target="_blank ">{{ item.title }}</a> -
    {{item.description}}
  </li>
  {% endfor %}
</ul>
{% endblock %} 